# Context System Architecture

```
┌─────────────────────────────────────────────────────────────────────┐
│                         Mile Platform                                │
└─────────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────────┐
│                    8 Context Types (EmbedContext)                    │
├─────────────────────────────────────────────────────────────────────┤
│                                                                       │
│  1. Tenant/Brand        │  2. User/Role        │  3. User State      │
│     • ID, Name          │     • ID, Email      │     • Progress      │
│     • Colors, Logo      │     • Role           │     • Completed     │
│     • Theme             │     • Permissions    │     • Saved Data    │
│     • Features          │     • Metadata       │     • History       │
│                         │                      │                     │
├─────────────────────────┼──────────────────────┼─────────────────────┤
│                         │                      │                     │
│  4. Environment         │  5. Permissions      │  6. Feature Flags   │
│     • Mode (live/test)  │     • Actions        │     • Flags Map     │
│     • Platform          │     • Limits         │     • Variant       │
│     • Device Type       │     • Policies       │     • Experiments   │
│     • Screen Size       │     • Tier           │     • Cohort        │
│                         │                      │                     │
├─────────────────────────┼──────────────────────┼─────────────────────┤
│                         │                      │                     │
│  7. Live Data           │  8. External         │                     │
│     • Metrics           │     • Integrations   │                     │
│     • Status            │     • Availability   │                     │
│     • Updates           │     • Fallbacks      │                     │
│     • Notifications     │     • Webhooks       │                     │
│                         │                      │                     │
└─────────────────────────┴──────────────────────┴─────────────────────┘
                                   │
                                   ▼
┌─────────────────────────────────────────────────────────────────────┐
│                    API Endpoints & Services                          │
├─────────────────────────────────────────────────────────────────────┤
│                                                                       │
│  POST /api/chat/embed-context/:sessionId                             │
│  ↓ Store context for session                                        │
│                                                                       │
│  POST /api/chat/message                                              │
│  ↓ Process with context → OpenAI Service                            │
│  ↓ Generate context-aware response                                  │
│                                                                       │
│  POST /api/component/generate                                        │
│  ↓ Use context → Component Generator                                │
│  ↓ Create context-aware component                                   │
│                                                                       │
└─────────────────────────────────────────────────────────────────────┘
                                   │
                                   ▼
┌─────────────────────────────────────────────────────────────────────┐
│                   Generated Component                                │
├─────────────────────────────────────────────────────────────────────┤
│                                                                       │
│  export const Component = ({ context, apiClient }) => {              │
│    // Extract context values                                        │
│    const brandColor = context.tenant?.brand?.primaryColor;          │
│    const userRole = context.user?.role;                             │
│    const hasPermission = (action) =>                                │
│      context.permissions?.allowedActions?.includes(action);         │
│                                                                       │
│    // Conditional rendering based on context                        │
│    if (userRole === 'admin') {                                      │
│      return <AdminView />;                                          │
│    }                                                                 │
│                                                                       │
│    // Apply branding from tenant context                            │
│    return (                                                          │
│      <div style={{ color: brandColor }}>                            │
│        {hasPermission('view') && <Data />}                          │
│      </div>                                                          │
│    );                                                                │
│  };                                                                  │
│                                                                       │
└─────────────────────────────────────────────────────────────────────┘
                                   │
                                   ▼
┌─────────────────────────────────────────────────────────────────────┐
│                      Reusability Benefits                            │
├─────────────────────────────────────────────────────────────────────┤
│                                                                       │
│  ✅ Same embed code works for multiple tenants                       │
│  ✅ Backend resolves context → Different rendering per tenant       │
│  ✅ No code changes needed for new tenants/users                    │
│  ✅ Dynamic adaptation to device, role, permissions                 │
│  ✅ Built-in A/B testing and feature flag support                   │
│                                                                       │
└─────────────────────────────────────────────────────────────────────┘

Example Flow:
═══════════

1. User A (Bank A, Customer, Mobile, Live)
   ↓ Sets context with Bank A branding, customer role, mobile device
   ↓ Generates component
   → Blue theme, limited features, mobile layout

2. User B (Bank A, Admin, Desktop, Live)  
   ↓ Same component, different context
   ↓ Admin role, desktop device
   → Blue theme, full features, desktop layout

3. User C (Bank B, Customer, Mobile, Live)
   ↓ Same component, Bank B context
   ↓ Different branding, customer role
   → Green theme, limited features, mobile layout

All using the SAME component code! 🎉
```
